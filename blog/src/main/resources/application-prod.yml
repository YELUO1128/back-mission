#druid 数据源配置
server:
  address: 172.16.0.15
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?userEncoding=true&useSSL=false&characterEncoding=utf-8
    username: root
    password: 5521292
  # 允许缓存，生产环境要开启
  thymeleaf:
    cache: true